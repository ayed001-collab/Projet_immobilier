[project]
name = "copilote-scoring"
version = "1.0.0"
description = "Moteur de scoring partagé (Home / Investment) — normalisation, pondération, décomposition explicable."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["scoring*"]
