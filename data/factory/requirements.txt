pandas>=2.1,<3
pytest>=8,<9
# Optionnels (chemins production) :
#   dagster>=1.7        # orchestration (dvf_pipeline/dagster_defs.py)
#   sqlalchemy>=2       # chargement PostGIS (outputs.load_postgis)
#   psycopg[binary]>=3  # driver Postgres
